# 台股系統全面檢查報告(2026-08-03 20:30 台北)

範圍:遠端分支/CI、後端程式、資料層(103 檔)、建置管線、線上前端(桌機+手機)。
方法:靜態審視 + `audit_data.py` 資料稽核 + `sweep.py` 全頁 Playwright 掃描 + 一次真實 `--update` 實測。

## 結果總覽

| 子系統 | 狀態 | 說明 |
|---|---|---|
| 遠端分支/CI | ✅ | main=gh-pages=chips 一致;CI 今日 16:53 原生建置上線(payload 08-03、集保 3 週);之後因 DATAFP 未變正確跳過 |
| 還原因子 | ✅ | 103 檔 × 1,374 個除權息/減資/分割事件,恆等式最大殘差 4.8e-10;最新因子全部 =1.0 |
| 存量分解 | ✅ | 103 檔外資+融資+借券 ≤ 發行股數,零超標 |
| 現金池口徑 | ✅ | tot = 法人 + 信用戶 + 殘差,抽樣 10 檔逐日重算全過 |
| chips JSON | ✅(修後) | schema/序列長度/RVPOS 全過(修正前 4+ 檔指標錯位,見 bug 1) |
| 建置管線 | ✅ | build_chips 103 檔 48 秒;CHIPSFP 穩定;push-chips 合併保護實測(104 檔進 → 103 檔出,無洗檔) |
| 前端(桌機) | ✅ | 六分頁零 JS 錯誤;13 個圖層/視窗鈕 → 14 個相異 sig 全部重繪;縮放中/停手皆 K 棒;VbP/現金池/成本分佈正常;總表與盤中區保留 |
| 前端(手機) | ✅ | 390px 無溢出;touch-action pan-y;雙指縮放 120→53;注音組字全程同一 DOM 節點、「鴻」正確篩出鴻海 |
| 燈號誠實性 | ✅ | 觀察用標示與回測結論(不上線)文案在線上確認 |

## 本次修掉的 4 個 bug

1. **chips 指標錯位(線上正在發生,稽核抓到)** — `indicators()` 過濾停牌列(close≤0),`build_one` 卻用未過濾日期對齊 → 有停牌日的股票(2317/2449/3036/3260…)ind 序列比主序列短,前端指標尾端整段錯位。改為由 indicators 回傳過濾後日期逐格對齊;修後 103 檔全過並已重推。
2. **存量分解每天下午會死(掃描抓到,用 08-03 未齊資料重現)** — 盤後分批公布(13:30 價格 → ~16:00 T86 → ~21:00 融資券 → ~22:00 外資持股),csRender 讀「最後一列」→ 下午時段全 null → 顯示「無發行股數資料」。改為每欄各自倒查最後非 null。
3. **`chip_upd` 表建立時機** — 只在全清單路徑建立,`--chips --sid` 在全新 DB 會炸並把整檔誤記失敗。改為無條件建立。
4. **起點追蹤與輪替預算的死迴圈** — 全域「起點已完成」旗標在 budget 模式永遠湊不齊 → 若日後調整 `chip_history_start`,每輪都對整批整段重抓、永不收斂。改為每檔追蹤(`chip_upd.start`),重抓隨輪替自然攤平、抓完即止。

## 調整

- 輪替預算 40→30:40×7×2輪/hr=560 已貼近 FinMind 600 次/hr;30 → 420 次/hr。**實測依據:本次沙盒 update 在 20:16 撞 402 rate-limit**(沙盒與 CI 共用同一 token,測試時要留意)。

## 已知狀態(非 bug,不需動作)

- **chips 資料日**:28 檔 08-03(含自選 15)、75 檔 07-31;CI 每 30 分輪 30 檔,今晚 22:00 前全數收斂,合併推送保證不回退。
- `intraday.json` 相對路徑 404:既有「本機採集器模式②」設計,線上由 catch 吃掉,只在開發者工具可見。
- 沙盒 DB 集保只有 1 週(正式 CI 快取有 3 週)→ 部署一律走「移植線上 payload」,`deploy_page.py` 已內建此邏輯與 schema 防呆。
- VbP(收盤落箱)與成本分佈(高低攤平)POC 可能略異:兩種標準演算法,說明文字已載明。

## CI 下一輪(20:30/21:00/21:30/22:00)預期行為

checkout main@1f2776b:budget 30 輪替 → 每輪 15 自選 + 15 輪替更到 08-03;21:00 融資券公布後 DATAFP 變 → 原生重建 index.html(含全部修正的樣板);push-chips 合併推送。若因沙盒耗掉額度遇 402,fm() 有 70 秒退避重試,timeout 120 分內可完成。
