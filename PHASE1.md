# twflow 自選股籌碼駕駛艙 — Phase 1 交付與確認

> 狀態:**Phase 1 資料層完成(含四項決議已執行),等待「進 Phase 2」指令**
> 日期:2026-08-02 · 實測環境:沙盒新建 DB,FinMind register + token

## 決議紀錄(2026-08-02)

| # | 議題 | 決議 | 執行狀態 |
|---|---|---|---|
| Q1 | 煞車線口徑 | **改台股慣例 60/120/240 標準 SMA** | Phase 2 實作;`btFlags` 的 `brk200/lose200`、CTA 觸線圈 ATR 冷卻參數需一併重校 |
| Q2 | 歷史長度 | **`chip_history_start` 拉到 2013-01-01** | ✅ 已回填,每檔 **3314 根日線 / 163 個月**(13.5 年) |
| Q3 | `mkt_daily` 歷史 OHL | **Phase 4 要用時再補**,現在只往前累積 | ✅ 欄位已加,不回補 |
| Q4 | chips 產生器位置 | **保持獨立 `build_chips.py`** | ✅ 已就位 |

### 拉到 2013 後才發現的問題(已修)

**FinMind 三大法人分類欄位在期間內改過兩次口徑**,直接照 2026 的欄名對映會靜默吃掉資料:

| 期間 | 官方分類 |
|---|---|
| ~2014-11 | `Foreign_Investor` / `Investment_Trust` / **`Dealer`(自營商未拆自行·避險)** |
| 2014-12 ~ 2017-12 | 上述 + `Dealer_self` / `Dealer_Hedging`(拆開),**無**外資自營商 |
| 2017-12-18 起 | 再加 `Foreign_Dealer_Self`(此前已含在 `Foreign_Investor` 內) |

若不處理,2013–2014 的自營商買賣超會整片變成 0(看起來像「自營商都不動」,實際是欄名沒對到)。
已修正為:legacy `Dealer` → `dealer_self`,`dealer_hedge` 留 **NULL(不是 0)**,並在 `chips/<sid>.json` 的
`caveat` 區塊標出各分類的分列起日。**外資合計一律 `f + (fd or 0)`、自營合計一律 `ds + (dh or 0)`**,跨口徑都成立。

另修:`CHIPSFP` 原本只雜湊 `(sid, last, n)`,這次「歷史加深 + 口徑修正」內容全變但指紋沒變(該推卻不會推)。
已改成雜湊**檔案實際內容**(排除 `gen` 時戳),同資料重跑指紋穩定、內容一變就變。

### ⚠ 順帶發現(既有 intraday 也受影響,本次未動)

`raw.githubusercontent.com` 的 `cache-control: max-age=300`,而且**查詢字串 `?v=` 無法穿透 CDN 快取**
——實測 force-push 後帶隨機 `?v=` 仍讀到舊內容,約 **5 分鐘後**才更新。
對每日籌碼無影響;但 `intraday.json` 每分鐘推送的設計,**實際上最多會有 5 分鐘延遲**。要不要處理請指示。

---

## 一、元件 → 依賴資料 → 台股對應

美股板 `#sec-chips` 共 12 個元件。逐一對照如下。
「狀態」欄:✅=Phase 1 資料已備齊 / ⏳=Phase 2–4 / ⛔=台股無此資料,已改設計。

### ⓪ 駕駛艙 `#cockpit`(五軌)

| 軌 | 美股依賴 | 台股資料來源 | 狀態 | 改造說明 |
|---|---|---|---|---|
| ① 主圖 K線 | `kline_SYM.json` 的 `o,h,l,c` | `chip_price` **× `chip_adj.adj_f`** | ✅ | **必用還原價**,否則 TD9/煞車線在除權息日亂跳 |
| ① 煞車線 ×4 | `t50/t100/t200`(不含當日的 49/99/199 日均)+ `t63`(63日前收) | 同演算法,套還原價 | ✅ | **已決議改台股慣例 60/120/240 標準 SMA**(Q1);觸線圈參數需同步重校 |
| ① TD9 | `C[i] vs C[i-4]`,不封頂 | 同 | ✅ | 照抄 |
| ① ▲▼燈號 | `btFlags`/`backtest` 6 條件 | 同結構,**門檻重新校準** | ⏳ P4 | Phase 4 回測通過才上 |
| ② 量 | `v`,琥珀 = `volr ≥ 1.75`(60日均) | `chip_price.volume` + **`chip_daytrade.dt_volume`** | ✅ | **新增分層**:真實換手 = `volume − dt_volume`(見下方單位確認),可切換總量/扣當沖 |
| ③ RVPOS + RSI | 20日實現波動 → 504日百分位(min_periods 60) | 同,套還原價 | ✅ | 900 根日線 → 前 ~524 根暖機,仍有 **376 根有效 RVPOS**,足夠 250 日視窗 |
| ④ score 檔位 | vs SMA50/100/200 + 63/126/252 日動能,6 項 ±1 | 同前三項;**後三項改為相對加權指數的超額動能** | ✅ 資料齊 / ⏳ P2 實作 | `chip_price` 已含 `TAIEX` 長史 |
| ⑤ 籌碼池累積 | Futu 大單/全單 proxy(`chipDaily()[d][sym].{m,r}`) | **5 軌官方數據**(外資/投信/自營/融資戶/借券) | ✅ 資料齊 / ⏳ P3 | 見下方 ② |

**全史月K**(`renderCockpitMax`):美股走 `kline_max_SYM.json`;台股改為 `chips/<sid>.json` 內的 `km[]`(**已用還原價聚合**,163 個月回溯至 2013-01)。

### ① 存量分解 `#chipstock`

| 美股 | 台股 | 狀態 |
|---|---|---|
| 流通股 = 13F 機構 + 非機構(**季頻**) | **多層日頻**:發行股數 `chip_foreign.issued`(官方) ├ 外資持股 `chip_foreign.shares`(官方,日) ├ 投信持股(投信買賣超累積**推估**) ├ 融資餘額 ×1000 股(官方,日) ├ 借券賣出餘額(官方,日) └ 其餘(主力+散戶+內部人,殘差) | ✅ 資料齊 / ⏳ P3 |
| — | 另軌:集保 400 張以上大戶持股比(週頻) | ⚠ **只有當週**,DB 逐週累積;沙盒新建 DB 目前 0 筆,正式環境靠 GH Actions 快取累加 |

**重疊警告**已納入設計:集保 400 張大戶**包含**外資保管銀行帳戶,與外資持股欄位重疊 → 兩軌並列、不做總和堆疊。

實測合理性(2026-07-31):

| 標的 | 發行股數 | 外資 | 融資 | 借券 | 合計佔比 |
|---|---|---|---|---|---|
| 2330 | 259.32 億股 | 69.2% | 0.11% | 0.06% | 69.4% ✅ |
| 2317 | 140.29 億股 | 40.2% | 0.36% | 0.57% | 41.2% ✅ |
| 2603 | 21.65 億股 | 21.6% | 0.68% | 3.38% | 25.6% ✅ |
| 2881 | 140.07 億股 | 18.4% | 0.05% | 0.28% | 18.8% ✅ |

各層加總 ≤ 發行股數,驗收條件成立。

### ② 籌碼池累積 `#chipflow`

| 軌 | 台股資料 | 方向口徑 |
|---|---|---|
| 外資 | `chip_inst.foreign_net` + `fdealer_net` | 買超 = +y |
| 投信 | `chip_inst.trust_net` | 買超 = +y |
| 自營商 | `chip_inst.dealer_self` + `dealer_hedge`(**分開存,可拆看**) | 買超 = +y |
| 融資戶 | Δ`chip_margin.margin_bal` × 收盤 | 增加 = +y |
| 借券賣出 | Δ`chip_sbl.sbl_bal` × 收盤 | **增加 = 賣壓 = −y**、回補 = +y |

疊加線:價格 % + **加權指數**(取代 SPY/QQQ),沿用 `shapeFit` 形狀對照縮放。判讀核心不變:**看背離**。

### ③ 成本分佈 `#chipcost`

| 美股 | 台股 | 狀態 |
|---|---|---|
| VbP / POC / 70% 值區 / 均價 / VWAP60/120 | 照抄,**用還原價** | ✅ 資料齊 / ⏳ P3 |
| 大戶均價 / 散戶均價(**逐筆**) | ⛔ register 擋 tick → 改**法人成本線**:外資/投信/融資戶 = Σ(買賣超股數 × 當日均價) ÷ Σ股數,當日均價用 `amount/volume` | ✅ 資料齊 / ⏳ P3 |

`chips/<sid>.json` 已提供 `am`(成交金額)與 `v`(成交量) → 前端可算當日均價,再乘 `af` 得還原均價。

### 其他元件

| 元件 | 台股對應 | 狀態 |
|---|---|---|
| `#scanpanel` 全市場掃描 | `mkt_daily` 已新增 `open/high/low` 三欄,**從今天開始累積** | ⏳ P4 |
| `#season` 季節性矩陣 | `km[]` 月K(還原價) | ⏳ P4;163 個月 = 13.5 年(已含 2015 股災、2018 貿易戰、2020 疫情、2022 空頭) |
| `#chipidx` / `#chipfidx` / `#chipoffs` | 照抄,**所有新按鈕必須進 `dataset.sig`** | ⏳ P2 |
| `#chipnote` | 逐條改寫成台股版(不直譯),已抄回美股原文作為權威底本 | ⏳ 各期同步 |

---

## 二、Phase 1 實際完成項目

### 資料層

- `config.json` 新增 `chip_history_start: "2013-01-01"`、`chip_json_days: 900`(**未動** `watch_history_start` / `flow_history_start`)
- `fetch_data.py` 新增 8 張 `chip_*` 表(與 `watch_*` 完全分離)+ `fetch_chip_history()` / `fetch_chip_events()` / `rebuild_chip_adj()` / `chip_history_all()`
- 增量:每表獨立記 `MAX(date)` 回看 7 天;**中斷可續**(逐表 upsert,不重跑整段)
- 新 CLI:`python3 fetch_data.py --chips [--sid 2330]`
- `mkt_daily` 加 `open/high/low`(TWSE `row[5..7]`、TPEX `row[4..6]`,**已用實際回應驗證欄序**)

回填實績:**15 檔 + TAIEX,每檔 3314 根日線(2013-01-02 ~ 2026-07-31),全程 3 分 40 秒、約 125 個 API 呼叫**(限額 600/hr)。
覆蓋度:價格/法人/融資券/外資持股/借券 全部回溯至 2013-01-02;**當沖 2014-01-06 起**(現股當沖上路日,此前無此制度)。

### 除權息還原(自建)

三種價格不連續事件全涵蓋:`TaiwanStockDividendResult`(除權息)、`TaiwanStockSplitPrice`(分割/面額變更)、`TaiwanStockCapitalReductionReferencePrice`(減資)。

- 因子 `f = after_price / before_price`,由今往回累乘,**錨定今天 = 1.0**
- **驗證**:2330 全史 35 個除權息日,`before_price` 與前一交易日收盤**完全相等**(35/35,零例外)
- **恆等式殘差**:`|adj − ((1+raw)/f − 1)|` 最大 **1e-8 %**(浮點精度)
- 量還原因子 `vol_f`:優先用官方 `NumberOfSharesIssued` **實測**事件前後股數跳變(門檻 0.5%),抓不到才退回解析式
  - 實測抓到:2603 減資 `sm=0.40`、3231 配股 `1.0864`、2881 連五年配股 `1.1535/1.05/1.05/1.05/1.025`、2882 `1.0482/1.1139`、3008 `0.98`
  - 2330 全期純現金股利 → `vol_f ≡ 1`(正確,量不需還原)

關鍵成果(2013-01-02 ~ 2026-07-31,13.5 年):

| 標的 | 事件數 | 除權息日跳動 未還原 | 還原後 | 最大機械式跳空 | 期初還原因子 | 63日動能平均偏差 |
|---|---|---|---|---|---|---|
| 2330 台積電 | 35 | 1.625% | **1.266%** | 3.77% | 0.6957 | 0.74pp(最大 4.86pp) |
| 2317 鴻海 | 15 | 5.538% | **2.061%** | 21.32% | 0.5112 | — |
| 2881 富邦金 | 19 | 4.437% | **1.210%** | 9.99% | **0.4280** | 1.67pp(最大 17.48pp) |
| 2603 長榮 | 12 | 17.859% | **4.033%** | 131.44% | 0.7111 | 3.62pp(最大 111pp) |

**2881 富邦金期初因子 0.428** —— 不還原的話,13.5 年的累積報酬會少算 **57%**;年年配股配息的金融股,
不做還原則所有均線、動能、TD9、成本分佈全部失真。這不是加分項,是必要條件。

### 分檔懶載入管線

- `build_chips.py` → `chips/<sid>.json`(每檔一個)
- 體積:**raw 148 KB / gzip 57 KB**(單檔;日線固定送 900 根,歷史加深不影響體積)。`raw.githubusercontent.com` 已驗證 `content-encoding: gzip` + `access-control-allow-origin: *`
- `deploy_github.py --push-chips`:**force-push 單一 commit** 到 `chips` 分支(照 `intraday` 分支手法,分支永遠只有 1 個 commit,不留歷史)
- `run_daily.py` 已接入,**獨立指紋 `CHIPSFP`**(存 `meta.chips_fp`),與 `index.html` 的 `DATAFP` 解耦——籌碼有新資料就推,index 沒變也不影響
- **已實測推送成功**,線上可讀:`https://raw.githubusercontent.com/groupuie/twflow/chips/2330.json`

`index.html` 體積增幅目前為 **0 KB**(長史完全不 inline),驗收條件 <300KB 有極大餘裕。

### JSON 編碼

| 欄位 | 內容 | 單位 |
|---|---|---|
| `d` | 交易日 | `YYYYMMDD` int |
| `o/h/l/c` | **未還原** OHLC | 元 |
| `af` | 還原因子(**變化點編碼**,只在事件日改變) | — |
| `vf` | 量還原因子(全 1 時整欄省略) | — |
| `v` / `am` | 成交量 / 成交金額 | 張 / 百萬元 |
| `inst.{f,fd,t,ds,dh}` | 外資/外資自營/投信/自營自行/自營避險 買賣超 | 張 |
| `mg.{m,s,mb,ms}` | 融資餘額/融券餘額/融資買/融資賣 | 張 |
| `fh.{r,sh,is}` | 外資持股率/持股數/發行股數(變化點) | % / 千股 |
| `sbl` / `msb` | 借券賣出餘額 / 券商融券餘額 | 張 |
| `dt.{v,b,s}` | 當沖量/買金額/賣金額 | 張 / 百萬元 |
| `km` | **全史月K(還原價)**,163 個月回溯至 2013-01 | — |
| `caveat` | 官方分類沿革(`fd_from`/`dh_from`/`dt_from`/`daily_window_clean`) | — |
| `ev` | 事件明細(日/類型/前後價/因子/配發/註) | — |
| `idx.c` | 加權指數收盤(對齊自身交易日) | 點 |
| `tdcc` | 集保週資料 | — |

**設計原則**:價格存未還原(總表顯示對得上看盤軟體),還原因子分開給 → 前端 `還原價 = c[i] × af[i]`。單一真相、不重複儲存。

### 當沖單位口徑(已確認)

`當沖成交股數 ÷ 成交股數` 中位數 0.213、p99 0.577、最大 0.808(2603,2021-08 航運狂潮)。
成交股數對每筆撮合只計一次(每股必有一買一賣),故當沖 Y 股對成交量的貢獻是 Y 而非 2Y。
→ **真實換手量 = `volume − dt_volume`**,直接相減即可,不需 ×2。

---

## 三、既有程式碼衝突點審視

**沒有阻斷性衝突**,但有 5 點要點名:

1. **`series_map()` 的 `D120` 切窗未受影響** — `chip_*` 與 `watch_*` 完全分離,`build_dashboard.py` 一行未改,總表建置速度與記憶體不變。✅
2. **`DATAFP` 機制保持有效** — 籌碼走獨立 `CHIPSFP`。無新資料時 index 不空推、時間戳不動;籌碼有新資料時只推 chips 分支。✅
3. **`deploy_github.py` 的 `.env` 優先順序未改** — `push_chips()` 沿用同一個 `_token()`,沙盒注入的假 `GITHUB_TOKEN=proxy-injected` 仍被 `.env` 蓋過。✅
4. **⚠ `mkt_daily` 加欄後 `MKT_COLS` 變 19 欄** — `store_snapshot()` 的 tuple 已同步加 3 個值。舊 DB 由 `ALTER TABLE` 遷移補欄(既有列為 NULL)。**歷史 OHL 要回補須重跑 `--backfill`(~40 分),建議排在假日或盤前低峰。**
5. **⚠ GH Actions 首跑時間** — `do_backfill()` 現在多了 `chip_history_all()`(+3.5 分)。目前首跑約 30–40 分,加上後仍 < 50 分驗收線,但餘裕變薄。若之後把 `chip_history_start` 往前拉,要重新估。

美股板讀碼時另發現 **20 個移植地雷**(完整清單在 `/tmp/us_chips_spec.md`),Phase 2 之前會逐一處理。最需要現在知道的三個:

- `cockpitRows()` 硬門檻 `bars.length < 260 → return null`;
- `initCustomUI()` 的代號驗證 `/^[A-Z.]{1,10}$/` **會擋掉純數字台股代號**;
- `renderCockpit` 的 `dataset.sig` 有 **18 個片段**,新增按鈕必須全部加進去。

---

## 四、剩下的開放問題

### Q5 — 集保大戶(400 張以上)那一軌,Phase 3 要不要就上?
`TaiwanStockHoldingSharesPer` 被 register 擋,現有 `fetch_tdcc()` 只抓得到**當週**。
正式環境的 DB(GH Actions 快取)已累積數週;沙盒是新建 DB,目前 **0 筆**。
- (A) Phase 3 就上,標「自建置日起累積」+ 估算徽章,序列短就短
- (B) 先留版位不畫,等累積到 ~26 週(半年)再開
建議 (A)——這是台股少數能看到「大戶 vs 散戶」的官方資料,即使短也比沒有好,誠實標註即可。

### Q6 — `intraday.json` 的 5 分鐘 CDN 快取要不要處理?
見上方「順帶發現」。若要即時性,得改走 GitHub Pages(`gh-pages` 分支,`max-age` 較短)或加 commit-sha 路徑。
不在本次範圍,但你可能想知道。

## 五、檔案清單

| 檔案 | 動作 |
|---|---|
| `config.json` | 改:+`chip_history_start`、+`chip_json_days` |
| `fetch_data.py` | 改:+8 表、+4 函式、+`--chips` CLI、`mkt_daily` 加 OHL |
| `build_chips.py` | **新增** |
| `deploy_github.py` | 改:+`push_chips()`、+`--push-chips` |
| `run_daily.py` | 改:+chips 建置與推送(獨立指紋) |
| `qa_adj.py` / `make_qa_report.py` / `qa_template.html` | **新增**(驗收工具,可不進 repo) |
| `qa_phase1.html` | 產出物:還原前後對照報告 |

**尚未推 main 分支**——等你確認後再推,避免線上 Actions 提早跑新流程。
`chips` 分支已推(孤兒分支,目前無人讀取,不影響線上)。
